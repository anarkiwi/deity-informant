name: Publish

# Publishes to PyPI via trusted publishing (OIDC). The maintainer must register
# the trusted publisher on PyPI: project deity-informant, owner anarkiwi,
# repo anarkiwi/deity-informant, workflow publish.yml, environment pypi.

on:
  push:
    tags: ["v*"]
  release:
    types: [published]

jobs:
  build-and-publish:
    runs-on: ubuntu-latest
    environment: pypi
    permissions:
      id-token: write
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-python@v6
        with:
          python-version: "3.12"
      - name: Build sdist + wheel
        run: |
          pip install build
          python -m build
      - name: Publish to PyPI
        uses: pypa/gh-action-pypi-publish@release/v1
        with:
          # A tag push and its GitHub release both fire this workflow; skip-existing
          # makes the second a no-op instead of a "File already exists" failure.
          skip-existing: true
